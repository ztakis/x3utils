# x3utils Windows GUI

Experimental Windows GUI prototype for x3utils.

This is a parallel project and does not replace the existing Windows scripts. It wraps the bundled OpenOCD files and currently covers only a small default flashing workflow.

Use the existing scripts for the supported workflow.
